{
  "name": "@hr-copilot/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "lint": "prisma validate",
    "test": "prisma validate && pnpm --filter @hr-copilot/shared build && node scripts/verify-shared-contract.cjs",
    "prisma": "prisma",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.2.3",
    "prisma": "^7.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "tsx": "^4.20.6"
  }
}
