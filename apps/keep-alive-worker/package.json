{
  "name": "@hr-copilot/keep-alive-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env dev",
    "deploy:dev": "wrangler deploy --env dev",
    "dev": "wrangler dev --env dev --test-scheduled",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0"
  }
}
